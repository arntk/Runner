---
wave: 3
depends_on: [01-01, 01-02, 01-03, 01-04]
files_modified: [backend/tests/isolation.test.js, analytics_engine/tests/test_isolation.py, .planning/phases/01-user-profile-foundation/01-VALIDATION.md, .planning/STATE.md]
autonomous: true
---

# Plan 01-05: Multi-user Data Isolation & E2E Verification (Wave 3)

**Goal**: Verify the system's ability to isolate user data and ensure the end-to-end profile foundation is robust.

## Data Isolation Verification

### Task 01-05-01: Backend Isolation Test
- <read_first> `backend/tests/isolation.test.js`
- <action> Implement test cases that: 1. Create User A and User B. 2. Verify User A cannot access User B's profile via `GET /api/profile`. 3. Verify User A cannot modify User B's profile via `PUT /api/profile`. 4. Ensure a 403 Forbidden or 401 Unauthorized is returned for unauthorized access attempts.
- <acceptance_criteria> `npm test backend/tests/isolation.test.js` passes.

### Task 01-05-02: Analytics Isolation Test
- <read_first> `analytics_engine/tests/test_isolation.py`
- <action> Write Pytest tests that: 1. Mock activities for two different `user_id` values. 2. Verify that `GET /activities?user_id=A` returns only User A's data. 3. Verify that the activity analysis logic correctly selects the profile associated with the provided `user_id`.
- <acceptance_criteria> `pytest analytics_engine/tests/test_isolation.py` passes.

## End-to-End Verification

### Task 01-05-03: E2E Profile Setup Flow
- <read_first> `.planning/phases/01-user-profile-foundation/01-VALIDATION.md`
- <action> Perform a complete E2E walkthrough using a fresh database state: Register a new user -> Complete the multi-step Onboarding flow (Step 1: Bio, Step 2: Zones) -> Verify the profile is persisted with the correct unit preferences -> Verify the Analytics engine is correctly calculating zones based on the VDOT formula for that user.
- <acceptance_criteria> Manual verification successful; mark requirement `FOUND-03` as PASSED in `01-VALIDATION.md`.

### Task 01-05-04: Cross-Service Integration Verification
- <read_first> `docker-compose.yml`
- <action> Audit the entire service stack. Ensure the Node.js backend can reach the Python analytics engine using the `http://analytics:5001` hostname within the Docker network. Confirm that the `JWT_SECRET` environment variable is identical across all services that require authentication (if shared) or that individual keys are correctly configured for secure communication.
- <acceptance_criteria> `docker-compose logs` shows no network or authentication errors during inter-service calls.

## Final Review

### Task 01-05-05: Update Project State
- <read_first> `.planning/STATE.md`
- <action> Update the global `STATE.md` to reflect Phase 1 completion. Move "Phase 1: User Profile Foundation" to the "Completed Phases" section and update the "Current Focus" to the next phase (Phase 2).
- <acceptance_criteria> `grep -r "Phase 1: Completed" .planning/STATE.md` returns true.

## Verification
- <automated> `npm run test:e2e` (Full suite)
- <automated> `pytest` (Full suite)
