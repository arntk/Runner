---
wave: 1
depends_on: [01-01]
files_modified: [analytics_engine/models.py, analytics_engine/ingest_strava.py, analytics_engine/api.py, analytics_engine/vdot.py, analytics_engine/ai_coach.py, analytics_engine/tests/test_vdot.py, analytics_engine/tests/test_user_isolation.py]
autonomous: true
---

# Plan 01-02: Python Analytics VDOT & Zone Service (Wave 1)

**Goal**: Update the analytics engine to handle multi-user data isolation and provide Jack Daniels VDOT-based zone estimation.

## Wave 0: Infrastructure & Testing

### Task 01-02-00: Initialize Test Stubs
- <read_first> `.planning/phases/01-user-profile-foundation/01-VALIDATION.md`
- <action> Create `analytics_engine/tests/test_vdot.py` and `analytics_engine/tests/test_user_isolation.py` with failing test cases for VDOT calculations and user-scoped data access.
- <acceptance_criteria> `pytest` runs and reports these tests as failed/pending.

## Multi-user Refactoring

### Task 01-02-01: Update Database Models
- <read_first> `analytics_engine/models.py`
- <action> Update `Activity` and `StravaAuth` SQLAlchemy models to include a non-nullable `user_id` column (UUID). Add `Profile` model that maps to the existing `profiles` table in PostgreSQL. Ensure all queries default to filtering by `user_id`.
- <acceptance_criteria> `grep -r "user_id" analytics_engine/models.py` returns multiple matches in class definitions.

### Task 01-02-02: User-Scoped Ingestion
- <read_first> `analytics_engine/ingest_strava.py`
- <action> Modify `ingest_strava_activities` function to accept a `user_id` as a required parameter. Update the `strava_auth` lookup and all subsequent database inserts to use this `user_id`.
- <acceptance_criteria> `grep -r ".filter_by(user_id=user_id)" analytics_engine/ingest_strava.py` returns matches.

### Task 01-02-03: User-Scoped API Endpoints
- <read_first> `analytics_engine/api.py`
- <action> Refactor all Flask endpoints (e.g., `/activities`, `/analyze`) to require a `user_id` query parameter. Update database queries to use `.filter_by(user_id=requested_user_id)` to ensure data isolation.
- <acceptance_criteria> `curl http://localhost:5001/activities?user_id=<UUID>` returns only that user's activities.

## Intelligence Services

### Task 01-02-04: Implement VDOT Service
- <read_first> `analytics_engine/ai_coach.py` (for zone definitions)
- <action> Create `analytics_engine/vdot.py`. Implement `estimate_zones(race_distance, race_time)` using the Jack Daniels VDOT formula. Output: 5 Pace zones (Recovery, Aerobic Base, Grey Zone, Threshold, VO2 Max). Support returning results in absolute (min/km) units.
- <acceptance_criteria> `pytest analytics_engine/tests/test_vdot.py` passes; VDOT 50 for a 5K (20:00) produces correct pace estimates.

### Task 01-02-05: Integrate Profile-based Analytics
- <read_first> `analytics_engine/ai_coach.py`
- <action> Update `analyze_activity` to fetch the user's `hr_zones` and `pace_zones` from the `Profile` model. Implement a conversion utility that converts relative units (% of Max HR, % of Threshold Pace) into absolute values (BPM, min/km) for the analysis logic.
- <acceptance_criteria> `grep -r "Profile.query" analytics_engine/ai_coach.py` returns matches; unit conversion logic is present.

## Verification
- <automated> `pytest analytics_engine/tests/test_vdot.py`
- <automated> `pytest analytics_engine/tests/test_user_isolation.py`
